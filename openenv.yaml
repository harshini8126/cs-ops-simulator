name: cs-ops-simulator

tasks:
  - name: easy
  - name: medium
  - name: hard

entrypoint:
  command: python app.py

endpoints:
  reset: /reset
  step: /step
  state: /state involving email classification, prioritization, and action decisions under time constraints.
